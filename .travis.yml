language: go
sudo: false

go:
  - 1.7

cache:
  directories:
    - $GOPATH/src/github.com/ready-steady/hdf5

install:
  - go get -d -t ./...
  - (cd $GOPATH/src/github.com/ready-steady/hdf5 && make install)

script:
  - make build
  - make test

notifications:
  email: false
